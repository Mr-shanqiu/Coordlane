interface:
  display_name: "Coordlane Core"
  short_description: "Dispatch isolated tasks with durable completion wakes"
  default_prompt: "Use $coordlane-core to register these Codex tasks, dispatch isolated work with explicit write paths, and receive durable results without polling."
